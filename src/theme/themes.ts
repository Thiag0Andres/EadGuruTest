import * as colors from "./colors";
import themes from "./themeDefault";
import FONT_WEIGHT from "./fonts";

const theme = {
  ...themes,
  FONT_WEIGHT,
  colors,
};

export default theme;
